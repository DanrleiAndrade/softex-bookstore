version: '3'

services:
  -sequelize:
    build: ./ts-Sequelize
    ports:
      - 3001:3001
    environment:
      - DB_USER=root
      - DB_PASS=password
      - DB_NAME=bookstore
      - DB_HOST=db
    volumes:
      - ./ts-Sequelize:/app-crud
    container_name: bookstore_crud
    restart: always
    depends_on:
      - db

  db:
    image: mysql
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=password
    restart: always
